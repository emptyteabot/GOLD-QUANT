# 夜间高价值信号规则（只推送，不下单）

## 核心目标
只推送“高确定性 + 高质量”的机会，减少噪音。

## 一级门槛（硬过滤）
- 宏观评分 >= 50
- 置信度 >= 0.75
- 信号强度 >= 0.60
- 共识度 >= 0.65
- 技能过滤评分 >= `SKILLS_MIN_SCORE`
- 宏观模式不是空仓

## 二级确认（结构性确认）
满足以下任意两项可加权：
- SMC 结构确认（OB / FVG / Liquidity Sweep）
- 趋势强度确认（ADX > 25）
- 波动率结构确认（ATR% 上行）

## 右侧浮盈加仓原则（仅作风控规则，不自动执行）
- 底仓 100%，浮盈 1R 后加 50%，趋势加速再加 25%
- 每次加仓必须同步上移止损，锁定前期利润

## 夜间执行模式
- `SIGNAL_ONLY = True`
- 只发飞书提示，不自动下单

## 说明
- 本规则用于筛出“高价值机会”，不保证盈利
- 禁止梭哈/无止损/情绪化操作
