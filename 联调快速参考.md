# 前后端联调 - 快速参考

## 🚀 快速启动

### 本地开发
```bash
# 一键启动
启动联调环境.bat

# 访问
前端: http://localhost:3000
后端: http://localhost:8000
API文档: http://localhost:8000/docs
```

### 测试联调
```bash
# 运行测试
scripts/test_integration.bat
```

### 停止服务
```bash
停止所有服务.bat
```

---

## 🌐 Railway部署

### 后端部署
```bash
cd backend
railway login
railway init
railway up

# 设置环境变量
ENVIRONMENT=production
ALLOWED_ORIGINS=https://your-frontend.railway.app
```

### 前端部署
```bash
cd frontend

# 更新.env.production
NEXT_PUBLIC_API_URL=https://your-backend.railway.app

# 部署
railway up
```

---

## 📡 API端点

| 端点 | 说明 |
|------|------|
| `GET /` | 系统信息 |
| `GET /health` | 健康检查 |
| `GET /api/ping` | 连通性测试 |
| `GET /api/market-status` | 市场状态 |
| `GET /api/instruments` | 标的列表 |
| `GET /api/quotes` | 实时行情 |
| `GET /api/klines` | K线数据 |
| `GET /api/signals` | 交易信号 |
| `GET /api/macro` | 宏观分析 |
| `GET /api/regime` | 行情识别 |

---

## 🔧 环境变量

### 前端 (.env.local)
```bash
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws
NEXT_PUBLIC_ENABLE_TRADING=false
NEXT_PUBLIC_DEBUG=true
```

### 后端 (.env)
```bash
ENVIRONMENT=development
ALLOWED_ORIGINS=http://localhost:3000
```

---

## ❌ 常见错误

### CORS错误
```bash
# 检查后端ALLOWED_ORIGINS配置
# 本地开发应该允许localhost:3000
```

### 连接失败
```bash
# 1. 检查后端是否启动
curl http://localhost:8000/health

# 2. 检查端口占用
netstat -ano | findstr :8000
```

### 数据不显示
```bash
# 打开浏览器开发者工具（F12）
# 查看Console和Network标签
```

---

## 📁 文件结构

```
黄金/
├── backend/
│   ├── app.py              # FastAPI应用
│   ├── requirements.txt    # Python依赖
│   └── railway.json        # Railway配置
├── frontend/
│   ├── .env.local          # 本地环境变量
│   ├── .env.production     # 生产环境变量
│   ├── utils/api.ts        # API客户端
│   └── railway.json        # Railway配置
├── scripts/
│   ├── test_integration.py # 测试脚本
│   └── test_integration.bat
├── docs/
│   └── 前后端联调指南.md   # 详细文档
├── 启动联调环境.bat         # 一键启动
└── 停止所有服务.bat         # 停止服务
```

---

## 📞 获取帮助

1. 查看详细文档: `docs/前后端联调指南.md`
2. 运行测试脚本诊断问题
3. 检查浏览器开发者工具
4. 查看后端日志

---

**提示**: 首次运行前端需要先 `npm install`
